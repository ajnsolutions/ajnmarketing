import { DashboardFeaturePlaceholder } from "@/components/dashboard/dashboard-feature-placeholder";

export const metadata = {
  title: "Notifications",
  description: "Stay on top of approvals, publishing, and review activity.",
};

export default function NotificationsRoute() {
  return <DashboardFeaturePlaceholder title="Notifications" />;
}
